// ============================================
// Shared Enums — used across multiple models
// ============================================

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum BranchType {
  MAIN
  SUB
  FRANCHISE
}

enum InstituteType {
  COMPUTER
  TYPING
  PARAMEDICAL
  OTHER
}

enum UserType {
  SUPER_ADMIN
  ORG_ADMIN
  ORG_STAFF
  BRANCH_ADMIN
  BRANCH_STAFF
  STUDENT
}

enum RoleScope {
  ORGANIZATION
  BRANCH
}
